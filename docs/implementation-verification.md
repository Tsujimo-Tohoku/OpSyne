# ローカル v0.1 の検証記録

実施日: 2026-09-15。Windows / Python 3.12.14 / uv 0.12.13。

## 自動検査

`python scripts/dev.py run --locked python scripts/check.py` が終了コード0で完了。

| 検査 | 結果 |
|---|---|
| Ruff lint / format | 成功（92ファイル） |
| mypy strict | 成功（55ファイル） |
| import-linter | 4契約すべて成功 |
| pytest | 230件すべて成功（73.16秒） |
| wheel / sdist ビルド | 成功 |

PR #1〜#3の更新を含むmainと未コミットの自動変換機能を組み合わせた隔離環境でも、
OpenAI SDK 3.13.0 / mypy 2.3.1 / httpx2 2.13.0で共通検査と230件のテストが成功した。
ローカルmainへ取り込んだ後、検証済みコピーと全114ファイルの内容の一致を確認した。
以下の実API記録はSDK 2.54.0での実測であり、SDK 3.13.0の互換性検査は模擬通信で行った。

主な試験は、原本・配送待ち・取得位置の再起動、変換の適用条件と未知状態、
根拠の範囲とマスク、OpenAI構造化出力、別主体承認、計画改変・期限・版・世代の拒否、
実行前台帳、結果不明時の再送防止、独立確認、バックアップ・途中復元の拒否、
Windowsでのプロセス排他、保存と監査が同時に失敗してもワーカーが継続することを扱う。
後から追加された証拠を古い計画の確認で解決せず、LLM処理中に対象が変わった提案も拒否する。

自動変換案の追加では、形式集約・標本の固定・二重予約防止・試行間隔・新しい証拠・回数上限・
未送信の予算待ち・再起動・自動生成オフを検査した。誤った参照パスや有効な定義との競合を拒否し、
案が通常調査を上書きしないこと、承認前の有効化や操作が起きないことも確認した。

配布物にWebのHTML/CSS/JavaScript、CLIエントリポイント、`py.typed` が含まれること、
`.env`・ローカルDB・キャッシュが含まれないことを検査した。

## ブラウザーで確認した範囲

隔離した合成データで、ログイン、役割切替、対象・観測源登録、Push取込、
変換承認・原本再解析、計画作成、自己承認拒否、別主体承認、操作実行、
独立確認後の案件解決を確認した。LLMキー不足のtaskは失敗として表示された。
HTMLを含むログは文字として表示され、ブラウザーでコードとして実行されなかった。
1280×720と390×844の画面を確認し、検証終了時のブラウザー警告・エラーはなかった。

標本検証APIと復元保留管理は自動テストで確認した。追加された標本検証ボタンの
実クリックと、復元保留解除のブラウザー操作は未確認（最終確認時にブラウザー接続が利用不可）。
自動提案の状態表示・再提案ボタンはJavaScript構文とコードレビューで確認し、追加UIの実クリックは未検証。

## 実LLMの追加検証

初期実装の検査後、ユーザー提供のAPIキーで `gpt-5.6-luna` の実接続を検証した。
合成ログから変換提案・調査・承認・操作・独立確認までの記録は [実LLM E2E](live-llm-e2e.md) に分けて記載する。

## 実環境での未確認範囲

通常テストのOpenAI APIと対象サービスへの通信は模擬応答を使う。
実アカウントでのLuna疎通は上記追加検証で確認済みだが、実サービスへの接続・実データの調査品質は未確認。
GitHub Actionsのリモート実行、Linuxでの実行、大規模負荷、複数ホストの隔離・障害対策も未確認。
この記録は[設計全体](opsyne-design-architecture.md)の本番適合を保証するものではない。

起動と設定は[利用手順](usage.md)、実装範囲と残る拡張は[開発計画](development-plan.md)を参照。
