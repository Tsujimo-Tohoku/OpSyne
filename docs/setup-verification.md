# 開発基盤の初期検証

実施日: 2026-09-15。対象: Windows、Python 3.12.14、uv 0.12.13。
これは開発基盤の検証記録です。製品機能の受入完了を示しません。

| 確認 | 結果 |
|---|---|
| `bootstrap.py --python <3.12実行ファイル>` | `.venv` 作成・22パッケージ導入成功 |
| `bootstrap.py` の再実行 | 成功。既存環境を再利用 |
| 新しい仮想環境への同期 | ローカルキャッシュを使い、オフラインで22パッケージ導入成功 |
| `uv lock --check` | pyprojectとlockが一致 |
| Ruff lint / format | 成功 |
| mypy strict | 15ソースファイルで問題なし |
| import-linter | 2契約とも成功 |
| pytest | インストール済みパッケージを別ディレクトリから使う試験1件成功 |
| `uv build --no-build-isolation` | wheel / sdist作成成功 |
| wheelの単独インストール | 別の仮想環境で10領域のimportと `py.typed` を確認 |
| 配布内容 | wheel 15ファイル、sdist 17ファイル。ローカルキャッシュ・仮想環境・秘密・実データなし |
| 設計資料 | 原本SHA-256一致、変更は記録済みのリンク置換・改行統一のみ |
| 文書・設定 | Markdownのローカルリンク、TOML / JSONの構文確認成功 |
| Git除外 | `.venv`、`.tools`、キャッシュ、`.env*`、ログ、DB等の除外を確認 |

## 依存境界の違反検出

製品ソースを変更せず、隔離コピーで以下を実行しました。全8ケースが期待通りの結果です。

- 空の骨格、各領域から `contracts` への依存: 許可。
- `contracts` から製品領域への依存: 拒否。
- 相対importによる他領域への依存: 拒否。
- 中間モジュール経由の間接依存: 拒否。
- 領域間の循環依存: 拒否。
- `TYPE_CHECKING` 内の越境import: 拒否。
- `contracts` 配下のサブモジュールから他領域への依存: 拒否。

## 実施範囲の限界

GitHubのリモートは未設定で、GitHub Actions自体とLinux上の実行は未確認です。
CIのWindows / Linuxジョブと使用するActionの版・参照は確認していますが、YAML専用パーサーでの検査は未実施です。
Pythonの自動ダウンロード経路は未実行で、既存の3.12実行ファイルを使用しました。
import検査は静的依存のみを扱い、実行時の権限分離を保証しません。

通常の再確認方法は[開発手順](development.md)を参照してください。
