# Read explicitly with: python -m opsyne serve --env-file .env
# Empty API key disables real LLM calls; deterministic collection and the demo still work.
OPENAI_API_KEY=
OPSYNE_DAILY_LLM_CALLS=20
OPSYNE_AUTO_INVESTIGATE=false
# Unknown JSON formats automatically produce unapproved, sample-validated adapter drafts.
OPSYNE_AUTO_ADAPTER_PROPOSALS=true
OPSYNE_ADAPTER_COALESCE_SECONDS=5
OPSYNE_ADAPTER_RETRY_SECONDS=900
OPSYNE_ADAPTER_MAX_ATTEMPTS=3

# Optional distinct bearer credentials named by CheckConfig.auth_env / Capability.auth_env.
# Register only the variable name in the UI/API; keep its value out of logs and JSON bodies.
OPSYNE_HTTP_CHECK_TOKEN=
OPSYNE_HTTP_OPERATION_TOKEN=

# Optional external intake. Keep the management UI/API on a private ingress.
OPSYNE_HOST=127.0.0.1
OPSYNE_ALLOWED_HOSTS=localhost,127.0.0.1,testserver
# --port > OPSYNE_PORT > hosting provider PORT > 8765
# OPSYNE_PORT=8765
# Only the actual reverse proxy may supply forwarded HTTPS/client information.
OPSYNE_TRUSTED_PROXIES=127.0.0.1
# Explicit owner-managed source bindings; passwords are read from named env vars.
# OPSYNE_HEROKU_DRAINS_FILE=/absolute/path/to/heroku-drains.json
# OPSYNE_HEROKU_DEMO_PASSWORD=<dedicated random value, 32-256 printable ASCII characters>
# OPSYNE_HEROKU_DEMO_TOKEN=<token returned by Heroku for this drain>
